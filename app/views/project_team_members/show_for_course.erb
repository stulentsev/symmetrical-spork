<div>
    <%= render :partial => :team_member_new %>
</div>

<div>
    <table id="team_project_members_table">
        <tr>
            <th><%= l(:name) %></th>
            <th><%= l(:function) %></th>
            <th><%= l(:description) %></th>
            <th><%= l(:curriculum) %></th>
            <th><%= l(:actions) %></th>
        </tr>
        <%= render :partial => :team_member_row, :collection => @members %>
    </table>

</div>