Relatorio <br/>
<%= select_tag :report_with_deadline_report_id,
               options_for_select(reports.map{|r| [r.name, r.id]},
                                  params[:report_id].to_i),
               :name => 'reports_with_deadline[report_id]' %>
